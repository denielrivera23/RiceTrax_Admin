import 'package:flutter/material.dart';

class Supplier extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Supplier'), backgroundColor: Colors.green[800]),
      body: Center(child: Text('Supplier')),
    );
  }
}